[project]
name = "daily-status"
version = "0.1.0"
description = "Daily work status from GitHub PRs and Jira tickets, posted to Slack"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.32.5",
]
